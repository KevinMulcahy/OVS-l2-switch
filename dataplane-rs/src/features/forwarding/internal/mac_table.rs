//! Placeholder for MAC learning table

pub struct MacTable;

impl Default for MacTable {
    fn default() -> Self {
        Self::new()
    }
}

impl MacTable {
    pub fn new() -> Self {
        Self
    }
}
